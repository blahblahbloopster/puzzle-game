package core

import core.Vars

/** The main game logic.  This updates the physics and mechanics. */
object Logic {

    fun update() {
        // Time since the last frame
        val delta = 1 / 60f  // todo: make this actually calculated instead of fixed

        Vars.world.tiles
    }
}
